# Data Cleaning Report

## Summary

- **File**: `/Users/GaryT/Documents/Work/AI/Recursive-Data-Cleaner/benchmarks/benchmark_data.jsonl`
- **Chunks processed**: 5
- **Functions generated**: 6
- **Total LLM time**: 545728ms
- **LLM calls**: 25

## Functions Generated

| # | Name | Description |
|---|------|-------------|
| 1 | `normalize_dates` | Converts various date string formats to ISO 8601 (YYYY-MM-DD). Handles: |
| 2 | `normalize_amounts` | Converts amount strings to decimal format without currency symbols or commas. Handles: dollar signs, USD suffixes, commas in numbers, and pure numeric strings. Returns string representation of decimal (e.g., "567.89"). Non-numeric inputs return None. Idempotent. |
| 3 | `normalize_phones` | Converts phone numbers to E.164 format (+1XXXXXXXXXX for US numbers). Preserves non-US numbers (e.g., +44...) as-is. Handles formats: (XXX) XXX-XXXX, XXX-XXX-XXXX, XXXXXXXXXX, +1XXXXXXXXXX. Strips all non-digit characters except leading '+' for international numbers. Returns None if invalid or empty. |
| 4 | `normalize_status` | Normalizes status field to one of: "active", "pending", "churned". Handles common typos like "actve", "pendng", "chruned", "ACTIVE", "Active". Case-insensitive matching. If status is null, empty, or not a recognized value, returns None. Idempotent. |
| 5 | `normalize_weights` | Converts weight values from various units (kg, lbs, g) to consistent "X.XX kg" format with two decimal places. Handles: numeric strings with unit suffixes (e.g., "6.0 kg", "13.2 lbs", "6000 g"), and unit-less numbers assumed to be in kg. Converts pounds to kg (1 lb = 0.453592 kg) and grams to kg (1 g = 0.001 kg). Strips whitespace, ignores non-numeric units. Returns None if invalid or empty. Idempotent. |
| 6 | `normalize_tags` | Converts tags field from mixed formats (list, comma-separated string, semicolon-separated string) into a consistent JSON array of lowercase strings. Handles: lists (already normalized), comma-delimited strings, semicolon-delimited strings. Strips whitespace from each tag and filters out empty values. Returns None if input is null or empty after cleaning. Idempotent. |

---

*Generated by Recursive Data Cleaner at 2026-02-11 15:23:02 UTC*